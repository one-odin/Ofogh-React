export type adTypes = {
    title: string,
    address: string,
    description: string,
}

export type adPropsTypes = {
    id?: string,
    title: string,
    address: string,
    description: string,
    mobile:number,
    lat: number,
    lng: number
}